<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<!--词条编辑记录信息-->
<mapper namespace="com.colin.foodsource.dao.EntryEditRecordMapper">

    <!--添加词条编辑记录-->
    <insert id="addEntryEditRecord" parameterType="com.colin.foodsource.model.EntryEditRecord">
        insert into op_entry_edit_record (
        entry_edit_record_id,
        food_entry_id,
        edit_content,
        edit_date,
        o_id_user,
        entry_state
        )
        VALUES (
        #{entryEditRecordId,jdbcType=VARCHAR},
        #{foodEntryId,jdbcType=VARCHAR},
        #{editContent,jdbcType=VARCHAR},
        #{editDate},
        #{oIdUser,jdbcType=VARCHAR},
        #{entryState,jdbcType=VARCHAR}
        )
    </insert>

    <!--提交记录-->
    <update id="commitRecord">
        update op_entry_edit_record set entry_state = '1' where entry_edit_record_id = #{entryEditRecordId}
    </update>
</mapper>
